# Retired — Cloudflare Worker

This Hono Worker previously exposed a cloud download/analyze API. It is **not deployed** and must not be restored as the primary product.

Use the Flutter client (`apps/client`) with bundled yt-dlp, or optionally run `apps/api` locally for developers.
